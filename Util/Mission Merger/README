Additional Things to do with the mission merger tool.

Add function/module for automatically transfering pbo files to servers
    - Publish module ?
	- Create test missions module ?
    - clone new repo then build pbo's in a temp directory